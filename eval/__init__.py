"""M3.2 evaluation harness: score apkdiff's class matches against real R8 ground truth.

Kept out of the `apkdiff` package on purpose (see the dev plan) — this is
tooling to grade the engine, not part of the engine itself.
"""
